apiVersion: v1
kind: Service
metadata:
  name: file-processor-service
  namespace: cloud-native-app
spec:
  selector:
    app: file-processor
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    name: http-metrics
  type: ClusterIP
